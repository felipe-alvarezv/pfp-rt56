pfp_category = {
    pfp_tohoku = {
        allowed = {
            NOT = {
                original_tag = JAP #Japan
            }
        }
        available = {
            is_subject = no
            #Kita-Tohoku
            custom_trigger_tooltip = {
                tooltip = pfp_tohoku_required_states_kita-tohoku
                has_full_control_of_state = 533 #Kita-Tohoku
            }
            #Minami Tohoku
            custom_trigger_tooltip = {
                tooltip = pfp_tohoku_required_states_minami_tohoku
                has_full_control_of_state = 1106 #Minami Tohoku
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 533 #Kita-Tohoku
                has_full_control_of_state = 1106 #Minami Tohoku
            }
            NOT = { has_global_flag = pfp_tohoku_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 533 #Kita-Tohoku
                state = 1106 #Minami Tohoku
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_tohoku_transfer_capital
            custom_effect_tooltip = pfp_tohoku_puppet
            custom_effect_tooltip = pfp_tohoku_occupation_type
            custom_effect_tooltip = pfp_tohoku_state_controller

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                #Transfer control and ownership of capital to formable puppet
                533 = { transfer_state_to = Q01 } #Kita-Tohoku

                #Formable puppet becomes a puppet of ROOT
                puppet = Q01

                #Code to assign generic portraits and set puppet color
                set_temp_variable = { pfp_portrait_tag = Q01 }
                pfp_set_generic_portrait = yes
                pfp_set_autonomy_color = yes

                #Formable puppet will core or contest its designated territory
                pfp_release_tohoku = yes

                #Transfer all technology from ROOT to formable puppet
                Q01 = { pfp_transfer_technology = yes }

                #Set global flag to prevent future decision use
                set_global_flag = pfp_tohoku_flag
            }
        }
        ai_will_do = { factor = 0 }
    }
}