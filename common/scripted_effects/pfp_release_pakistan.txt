#Scripted effect to release Pakistan (P75)
pfp_release_pakistan = {
	hidden_effect = {
		#If resistance mechanics are enabled, formable puppets will contest and control its corresponding states.
		if = {
			limit = { has_global_flag = pfp_management_resistance_enabled }

			#Add contestion to formable puppet
			442 = { add_contested_owner = P75 } #Sarhad
			444 = { add_contested_owner = P75 } #Balochistan
			445 = { add_contested_owner = P75 } #Quetta
			1093 = { add_contested_owner = P75 } #West Sind

			#Add claims to formable puppet
			442 = { add_claim_by = P75 } #Sarhad
			444 = { add_claim_by = P75 } #Balochistan
			445 = { add_claim_by = P75 } #Quetta
			1093 = { add_claim_by = P75 } #West Sind

			#Set formable puppet as controller of contested state
			every_state = {
				limit = { has_contested_owner = P75 }
				
				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }
					transfer_state_to = P75
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					set_state_controller_to  = P75
				}
			}

			#Set custom focus tree to combat resistance
			P75 = { load_focus_tree = pfp_formable_puppet_focus }
		}
		#If resistance mechanics are disabled, formable puppets will control and core its corresponding states.
		else = {
			#Add cores to formable puppet
			442 = { add_core_of = P75 } #Sarhad
			444 = { add_core_of = P75 } #Balochistan
			445 = { add_core_of = P75 } #Quetta
			1093 = { add_core_of = P75 } #West Sind

			#Set formable puppet as owner and controller of core state
			every_state = {
				limit = { is_core_of = P75 }

				#If ROOT owns the state, transfer control and ownership to formable puppet
				if = { 
					limit = { is_owned_by = ROOT }

					add_contested_owner = P75
					transfer_state_to = P75
				}

				#If ROOT does not own the state, transfer control to formable puppet
				else = {
					add_contested_owner = P75
					set_state_controller_to = P75
				}
			}

			P75 = {
				remove_ideas = {
					pfp_puppet_resistance_idea_1
				}
			}
		}
	}
}
