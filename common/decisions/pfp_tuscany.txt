pfp_category = {
    pfp_tuscany = {
        allowed = {
            NOT = {
                original_tag = ITA #Italy
                original_tag = TOS #Tuscany
            }
        }
        available = {
            is_subject = no
            #Toscana
            custom_trigger_tooltip = {
                tooltip = pfp_tuscany_required_states_toscana
                has_full_control_of_state = 162 #Toscana
            }
        }
        visible = {
            OR = {
                has_full_control_of_state = 162 #Toscana
            }
            NOT = { has_global_flag = pfp_tuscany_flag }
        }
        highlight_states = {
            highlight_state_targets = {
                state = 162 #Toscana
            }
        }
        icon = generic_military
        complete_effect = {
            custom_effect_tooltip = pfp_tuscany_core_states
            custom_effect_tooltip = pfp_tuscany_transfer_states
            custom_effect_tooltip = pfp_tuscany_puppet

            #Create formable puppet with all its states and then puppet
            hidden_effect = {
                pfp_release_tuscany = yes
                set_autonomy = { target = P95 autonomy_state = autonomy_military_administration }

                #Code to assign generic portraits
                set_temp_variable = { pfp_portrait_tag = P95 }
                pfp_set_generic_portrait = yes
            }
        }
        ai_will_do = { factor = 0 }
    }
}